import { getStaticProps } from "next/dist/build/templates/pages";
import verifyToken from "./verifyToken";

const decodeLoginCookie = (WrappedComponent: React.ReactNode) => {
    
}

export default decodeLoginCookie